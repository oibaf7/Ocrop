[package]
name = "agent"
version = "0.1.0"
edition = "2024"

[dependencies]
serde_json = "1.0.149"
shared = { path = "../shared" }
config = "0.15.22"
serde = { version = "1.0.228", features = ["derive"] }
