[package]
name = "agent"
version = "0.1.0"
edition = "2024"

[dependencies]
shared = { path = "../shared" }
pulse = { path = "../pulse" }
config = "0.15.22"
serde = { version = "1.0.228", features = ["derive"] }